package fib;
/**
 * The Fibonacci numbers are defined recursively:
 * fib(0) = 0
 * fib(1) = 1
 * fib(n) = fib(n-2) + fib(n-1) for n > 2
 * 
 * implement the computation of fib(n)
 * with a recursive solution
 * and with a loop.
 * 
 * @author kleinen
 *
 */
public class Fib {

	public static long fib(int n) {
		return -1;
	}

	public static long fibLoop(int n) {
		return -1;
	}

}
